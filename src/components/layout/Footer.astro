---
import Container from '../ui/Container.astro';

const currentYear = new Date().getFullYear();

const footerLinks = {
  product: [
    { label: 'Features', href: '#features' },
    { label: 'Integrations', href: '#integrations' },
    { label: 'Pricing', href: '#pricing' },
    { label: 'Changelog', href: '#changelog' },
  ],
  company: [
    { label: 'About', href: '#about' },
    { label: 'Blog', href: '#blog' },
    { label: 'Careers', href: '#careers' },
    { label: 'Contact', href: '#contact' },
  ],
  helpAndSupport: [
    { label: 'Documentation', href: '#docs' },
    { label: 'Trust Center', href: '#trust' },
    { label: 'FAQs', href: '#faqs' },
    { label: 'Status', href: '#status' },
  ],
  ourTeam: [
    { label: 'Team', href: '#team' },
    { label: 'Investors', href: '#investors' },
    { label: 'Open Roles', href: '#jobs' },
    { label: 'Press Kit', href: '#press' },
  ],
};

const socialLinks = [
  {
    label: 'Twitter',
    href: 'https://twitter.com/cleric',
    icon: `<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>`
  },
  {
    label: 'LinkedIn',
    href: 'https://linkedin.com/company/cleric',
    icon: `<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>`
  },
];
---

<footer class="bg-dark text-white py-12 tablet:py-16">
  <Container>
    <!-- Main Footer Content -->
    <div class="grid grid-cols-2 tablet:grid-cols-3 desktop:grid-cols-6 gap-8 mb-12">
      <!-- Brand Column -->
      <div class="col-span-2">
        <a href="/" class="inline-block mb-4">
          <span class="text-xl font-semibold">Cleric</span>
        </a>
        <p class="text-gray-400 text-sm max-w-xs">
          The AI SRE that learns from your engineers.
        </p>
      </div>

      <!-- Product Links -->
      <div>
        <h4 class="text-sm font-semibold text-white mb-4">Product</h4>
        <ul class="space-y-3">
          {footerLinks.product.map(({ label, href }) => (
            <li>
              <a href={href} class="text-gray-400 hover:text-white transition-colors text-sm">
                {label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Help & Security Links -->
      <div>
        <h4 class="text-sm font-semibold text-white mb-4">Help & Security</h4>
        <ul class="space-y-3">
          {footerLinks.helpAndSupport.map(({ label, href }) => (
            <li>
              <a href={href} class="text-gray-400 hover:text-white transition-colors text-sm">
                {label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Our Team Links -->
      <div>
        <h4 class="text-sm font-semibold text-white mb-4">Our Team</h4>
        <ul class="space-y-3">
          {footerLinks.ourTeam.map(({ label, href }) => (
            <li>
              <a href={href} class="text-gray-400 hover:text-white transition-colors text-sm">
                {label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Company Links -->
      <div>
        <h4 class="text-sm font-semibold text-white mb-4">Company</h4>
        <ul class="space-y-3">
          {footerLinks.company.map(({ label, href }) => (
            <li>
              <a href={href} class="text-gray-400 hover:text-white transition-colors text-sm">
                {label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="pt-8 border-t border-gray-800">
      <div class="flex flex-col tablet:flex-row justify-between items-center gap-4">
        <div class="flex items-center gap-4">
          <!-- Social Links -->
          {socialLinks.map(({ label, href, icon }) => (
            <a
              href={href}
              class="text-gray-400 hover:text-white transition-colors"
              aria-label={label}
              target="_blank"
              rel="noopener noreferrer"
            >
              <Fragment set:html={icon} />
            </a>
          ))}
        </div>
        <p class="text-gray-400 text-sm">
          © {currentYear} Cleric, Inc. All rights reserved.
        </p>
      </div>
    </div>
  </Container>
</footer>
